import { getCurrentRouletteRound,subscribeToRouletteRounds } from "../../repositories/rouletteRepository.js";
import { getRouletteSegments } from "../../data/roulettePrizes.js";
import { paintRouletteWheel,rotationForSegment,spinWheel } from "../../utils/rouletteAnimation.js";
const $=s=>document.querySelector(s);const segments=getRouletteSegments();let rotation=0,lastRound=null;
async function render(round){const section=$("#screen-roulette");if(!section)return;section.hidden=!round;if(!round)return;$("#screen-roulette-state").textContent=round.status==="open"?"Lista para girar":round.status==="spun"?"Resultado":"Cerrada";$("#screen-roulette-participant").textContent=round.active_display_name||"Esperando participante";if(round.result_segment_index!=null&&lastRound!==round.id){lastRound=round.id;rotation=rotationForSegment(Number(round.result_segment_index),segments.length,rotation);$("#screen-roulette-result").textContent="Girando…";await spinWheel($("#screen-roulette-wheel"),rotation);$("#screen-roulette-result").textContent=round.result_label||"Giro finalizado";}else if(round.result_label)$("#screen-roulette-result").textContent=round.result_label;}
export async function initRouletteScreen(){paintRouletteWheel($("#screen-roulette-wheel"),segments);try{await render(await getCurrentRouletteRound())}catch(e){console.warn(e)}await subscribeToRouletteRounds(async()=>render(await getCurrentRouletteRound()));}
