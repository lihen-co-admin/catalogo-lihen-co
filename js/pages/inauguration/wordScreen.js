import {getCurrentWordRound,subscribeToWordRounds} from "../../repositories/wordGameRepository.js";import{createWordTicker,formatWordTime}from"../../utils/wordTimer.js";const $=s=>document.querySelector(s);let round=null;function render(r){round=r;const shell=$("#screen-word");if(!shell)return;shell.hidden=!r;$("#screen-word-state").textContent=r?.status==="open"?"Ronda abierta":r?.status==="closed"?"Ronda cerrada":"En espera";$("#screen-word-hint").textContent=r?.hint_text||"Esperando una pista";$("#screen-word-mask").textContent=r?.masked_word||"_ _ _ _ _";$("#screen-word-result").textContent=r?.winner_display_name?`Ganó: ${r.winner_display_name}`:r?.status==="closed"?"Ronda finalizada":"";}export async function initWordScreen(){try{render(await getCurrentWordRound())}catch(e){console.warn(e)}await subscribeToWordRounds(async()=>render(await getCurrentWordRound()));createWordTicker(()=>round,s=>{$("#screen-word-clock").textContent=formatWordTime(s)});}
